server:
  port: 8081
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

spring:
  application:
    name: user-service
  cloud:
    nacos:
      discovery:
        enabled: false
        register-enabled: false
      config:
        enabled: false

jwt:
  secret: travel-platform-jwt-secret-key-2024-must-be-at-least-256-bits-long-for-hs256-algorithm
  expiration: 604800000
